<Window x:Class="PasteViews.MVVM.View.PasteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasteViews.MVVM.View"
        mc:Ignorable="d"
               Title="Paste Views" Height="650" Width="900">
    <Border Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Border>
                <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" CanUserReorderColumns="False" ItemsSource="{Binding ViewsToPaste, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn IsReadOnly="False" Header="View Name" Width="*" Binding="{Binding Name}"/>
                        <DataGridTextColumn IsReadOnly="True" Header="Assosiated Level" Width="*" Binding="{Binding Level.Name}"/>
                        <DataGridTextColumn IsReadOnly="True" Header="Level Elevation" Width="*" Binding="{Binding Elevation}"/>
                        <DataGridTextColumn IsReadOnly="True" Header="View Type" Width="*" Binding="{Binding viewType}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <UniformGrid Grid.Row="1" Rows="1">
                <StackPanel VerticalAlignment="Center">
                    <RadioButton Content="Create Missing Levels" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 0 10" IsChecked="{Binding CreateLevels}"/>
                    <RadioButton Content="Ignore Missing Levels" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Width="150" Content="Clear Copied Views" Height="30" Command="{Binding clearBTN}"/>
                <Button Width="150" Content="Paste Copied Views" Height="30" Command="{Binding pasteBTN}"/>
                <Image Source="/PasteViews;component/Images/logo.png" Width="150" />


            </UniformGrid>
        </Grid>

    </Border>
</Window>